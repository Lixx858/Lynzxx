import {NextResponse} from 'next/server';import {createHmac} from 'crypto';
export async function POST(req){const {username,password}=await req.json();if(username!==process.env.ADMIN_USERNAME||password!==process.env.ADMIN_PASSWORD)return NextResponse.json({error:'Username atau password salah.'},{status:401});const payload=Buffer.from(JSON.stringify({u:username,t:Date.now()})).toString('base64url');const sig=createHmac('sha256',process.env.SESSION_SECRET).update(payload).digest('hex');const res=NextResponse.json({ok:true});res.cookies.set('ixb_admin',payload+'.'+sig,{httpOnly:true,secure:true,sameSite:'lax',path:'/',maxAge:60*60*24*7});return res}
