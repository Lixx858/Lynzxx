import {openAIChat} from "../../../../lib/openai"; export async function POST(req){try{const b=await req.json(),t=String(b.transcript||"").slice(0,120000),n=Math.min(Math.max(Number(b.count||10),1),30);if(!t.trim())return Response.json({error:"Transcript kosong."},{status:400});const prompt=`Analyze this timestamped transcript and return ONLY JSON: {"clips":[{"start":number,"end":number,"score":number,"title":string,"hook":string,"reason":string,"caption":string,"hashtags":string[]}]}. Find up to ${n} self-contained short-form moments. Prioritize strong hooks, curiosity, emotion, useful insight, punchlines and context. Never guarantee virality.\n\nTRANSCRIPT:\n${t}`;let x=await openAIChat([{role:"system",content:"You are an expert short-form video editor."},{role:"user",content:prompt}]);x=x.replace(/^```json\s*/i,"").replace(/```$/,"").trim();return Response.json(JSON.parse(x))}catch(e){return Response.json({error:e.message},{status:500})}}