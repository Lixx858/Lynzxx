export async function POST(req){try{const{url}=await req.json();if(!/^https?:\/\//i.test(String(url||"")))return Response.json({error:"Link video tidak valid."},{status:400});const worker=process.env.VIDEO_WORKER_URL;if(!worker)return Response.json({error:"VIDEO_WORKER_URL belum dikonfigurasi. Deploy worker yang ada di folder worker/ lalu masukkan URL-nya di Vercel."},{status:400});const r=await fetch(worker.replace(/\/$/,"")+"/process-url",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url})});const text=await r.text();let d;try{d=JSON.parse(text)}catch{d={error:text.slice(0,500)}}if(!r.ok)return Response.json(d,{status:r.status});return Response.json(d)}catch(e){return Response.json({error:e.message},{status:500})}}